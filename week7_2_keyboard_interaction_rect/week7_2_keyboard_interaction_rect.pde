float x;
float y;
float w = 100;
float h = 50;
boolean isEnterPressed = false;
void setup() {
  size(600, 600);
}
void draw() {
  background(0);
  x = width;
  y = 60;

    textSize(64);
    text("SOMETHING IS MOVING AROUND", x, y);
  
}